# NEXT

1. Verify that overlap protection fully eliminates duplicate sends during heavy backfill.
2. Add optional bootstrap mode to mark historical messages as seen without forwarding them all on first run.
3. Add webhook delivery retry/backoff with durable failed-delivery queue.
4. Expand media handling for video/audio/document payloads in downstream consumers.
5. Update project docs to include debug workflow using `/api/debug/store` and `data/engine.log`.
